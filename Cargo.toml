[package]
name = "iot_app"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { version = "0.7.0", features = [] }
rand = "0.8"

[features]
default = ["web"]
web = ["dioxus/web"]
desktop = ["dioxus/desktop"]
mobile = ["dioxus/mobile"]